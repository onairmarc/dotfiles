# {{MODULE_NAME}}

{{GEN:one paragraph on this module's purpose — the problem it solves within the larger system, authored from its manifest and confirmed with the user for deep-dive
modules.}}

## Structure

{{GEN:a short tree or table of the module's top-level directories, each with a one-line role. Read the module tree; do not invent paths.}}

## Key Components

{{GEN:a table (component | source file | description) of the module's significant components. For a skeleton, list the top-level entry points; for a deep-dive module,
list every component an agent must know.}}

## Running / Testing

{{GEN:the run/test commands specific to this module when they differ from the root `{{TEST_COMMAND}}`. If they do not differ, state that the root commands apply and omit a
separate command block.}}
