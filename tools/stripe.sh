#source "$HOME/.stripe"
#fpath=("$HOME/.stripe" $fpath)
#autoload -Uz compinit && compinit -i

#if [ -d "$ZSH/completions" ]; then
#  rm -rf   "$ZSH/completions"
#fi

#mkdir -p "$ZSH/completions"
#stripe completion
#mv stripe-completion.sh > "$ZSH/completions/.stripe"

