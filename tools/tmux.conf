# Change the prefix to Ctrl-Space
set -g prefix C-Space

# Unbind the default prefix (Ctrl-b) to avoid conflicts
unbind C-b

# Reload configuration with prefix + r (optional for convenience)
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"
